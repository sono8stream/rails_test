	<p>
		<strong>Title:</strong>
		<%= @article.title %>
	</p>
	
	<p>
		<strong>Text:</strong>
		<%= @article.text %>
	</p>

	<h2>コメント</h2>
	<%= render @article.comments %>

	<h2>コメントを書く</h2>
	<%= render 'comments/form' %> <%# views/comments/_form.html.erbを参照 %>

	<%= link_to 'Edit', edit_article_path(@article) %>
	<%= link_to 'Back', articles_path%>